apiVersion: v1
kind: Service
metadata:
  name: {{ include "yourballot.fullname" . }}
  labels:
    {{- include "yourballot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yourballot.selectorLabels" . | nindent 4 }}
