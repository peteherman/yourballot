apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yourballot.fullname" . }}-test-connection"
  labels:
    {{- include "yourballot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yourballot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
